{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Community Post Flagged Event",
  "description": "Publicado pelo módulo community quando um post é sinalizado (moderação). Consumível para alertas de moderação.",
  "type": "object",
  "properties": {
    "tenant_id": { "type": "string" },
    "post_id": { "type": "string" },
    "user_id": { "type": "string" },
    "reason": { "type": "string" }
  },
  "required": ["tenant_id", "post_id", "user_id"]
}
