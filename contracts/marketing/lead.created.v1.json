{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Lead Created Event",
  "description": "Publicado pelo lead-intake-gateway em toda criação de novo lead. Tópico legado sem prefixo de domínio (lead.created.v1).",
  "type": "object",
  "properties": {
    "tenant_id": { "type": "string", "format": "uuid" },
    "lead_id": { "type": "string", "format": "uuid" },
    "session_id": { "type": "string", "format": "uuid" }
  },
  "required": ["tenant_id", "lead_id", "session_id"]
}
