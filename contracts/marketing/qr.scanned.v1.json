{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "QR Scanned Event",
  "description": "Publicado pelo lead-intake-gateway quando um toque type=qr.scanned é registrado. Tópico legado sem prefixo de domínio (qr.scanned.v1).",
  "type": "object",
  "properties": {
    "tenant_id": { "type": "string", "format": "uuid" },
    "session_id": { "type": "string", "format": "uuid" },
    "lead_id": { "type": "string", "format": "uuid" },
    "ref": { "type": "string" }
  },
  "required": ["tenant_id", "session_id", "lead_id", "ref"]
}
